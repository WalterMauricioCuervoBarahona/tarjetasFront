export interface generateRandomCarga {
    numberoTarjeta: number;
    ultimaCarga: number;
    saldoFinal: number;
}

export interface generateRandomUso {
    numberoTarjeta: number;
    ultimoUso: number;
    saldoFinal: number;
}